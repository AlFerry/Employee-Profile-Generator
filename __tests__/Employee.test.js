const { it } = require('jest-circus');
const { describe } = require('yargs');
const Employee = require('../lib/Employee');

describe("Employee", () => {

    // Methods
    describe("Initialization / Constructor", () => {

        // Testing of object properties after constructing a new instance
        it("should build with prop x when given y", () => {

        })

    });
    describe("getName", () => {

        // Arrange
        // Create a new employee object

        // Act
        // Call the getName method and store result

        // Assert
        // The result is equal to an expected value

    });

    describe("getId", () => {

    
    });

    describe("getEmail", () => {

    
    });

    describe("getRole", () => {

    
    });

});